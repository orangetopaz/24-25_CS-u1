#include <iostream>
#include <ctime>

void drawBoard(char *spaces);  // the astrix allows the var spaces to be a pointer (an address for another variables location). This one will keep track of what spots are taken / not
void playerMove();